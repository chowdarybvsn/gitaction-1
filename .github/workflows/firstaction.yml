name: First workflow
on: push
jobs:
  first-job: 
    runs-on: ubuntu-latest
    steps:
       - name: Print Greeting
         run: echo "!Hello Morning Mano!"
       - name: Print GoodBye
         run: echo "{{ github }}"
